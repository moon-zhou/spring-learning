spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/moon_jwt?useUnicode=true&characterEncoding=utf-8&serverTimezone=UTC

    username: root
    password: root
mybatis:
  config-location: classpath:mybatis.xml
debug: true
server:
  port: 8888